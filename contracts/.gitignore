# Foundry build artifacts
out/
cache/

# Env — real secrets go here, never commit
.env

# Broadcast logs contain deployed addresses and tx hashes — exclude by default
# Remove this line if you want to commit deployment receipts
broadcast/*/31337/
broadcast/**/dry-run/
